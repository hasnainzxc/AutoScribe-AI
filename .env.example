# Copy to `.env` and fill in values

# Reddit API (PRAW)
REDDIT_CLIENT_ID=your_client_id
REDDIT_CLIENT_SECRET=your_client_secret
# A descriptive UA helps Reddit identify your app; format: appname/version by user
REDDIT_USER_AGENT=ContentPipeline/0.1.0 by your_reddit_username
# Optional: if using the password grant (script app), provide these
# REDDIT_USERNAME=your_reddit_username
# REDDIT_PASSWORD=your_reddit_password

# OpenRouter (OpenAI-compatible) — used for dialogue generation
OPENROUTER_API_KEY=your_openrouter_api_key

# Optional: OpenAI-compatible TTS server (Chatterbox/Orpheus/etc.)
# If not set, the pipeline falls back to Google gTTS.
# CHATTERBOX_BASE_URL=http://localhost:8014
# CHATTERBOX_API_KEY=your_tts_server_key
# OPENAI_BASE_URL=http://localhost:8014/v1
# OPENAI_API_KEY=your_tts_server_key
# TTS_MODEL=gpt-4o-mini-tts
# TTS_DEFAULT_VOICE=alloy
# TTS_DJCARA_VOICE=Non_Stop_Pop.mp3
# TTS_RESPONSE_FORMAT=mp3
# Optional TTS tuning (defaults used by server if omitted)
# TTS_TEMP=0.6
# TTS_EXAG=0.9
# TTS_CFG=0.3
# TTS_SPEED=1.0
# TTS_REQUIRE_SERVER=1  # set to require server TTS and disable gTTS fallback

# DJ Cara tuned defaults ("ours")
# TTS_DJCARA_TEMP=0.7
# TTS_DJCARA_EXAG=0.8
# TTS_DJCARA_CFG_WEIGHT=0.3
# TTS_DJCARA_SEED=42
# TTS_DJCARA_SPEED_FACTOR=1.0  # or TTS_DJCARA_SPEED
# Optional: chunk size if your server benefits from smaller chunks
# TTS_DJCARA_CHUNK_SIZE=100

# Advanced Chatterbox timeouts (tune for longer jobs)
# CHATTERBOX_POST_CONNECT_TIMEOUT=3
# CHATTERBOX_POST_READ_TIMEOUT=10
# CHATTERBOX_POLL_TIMEOUT=180
# CHATTERBOX_POLL_INTERVAL=1.0
# CHATTERBOX_VERBOSE_POLL=1   # log latest server file (name/size/modified) while polling
# CHATTERBOX_GET_CONNECT_TIMEOUT=3   # outputs/download GET connect timeout
# CHATTERBOX_GET_READ_TIMEOUT=6      # outputs GET read timeout
# CHATTERBOX_DOWNLOAD_RETRIES=5      # attempts to download final WAV if the first try fails

# Legacy Orpheus variable names (supported as aliases)
# ORPHEUS_API_URL=http://localhost:5005/v1
# ORPHEUS_MODEL_NAME=Orpheus-3b-FT-Q8_0.gguf
# ORPHEUS_PORT=5005
# ORPHEUS_HOST=0.0.0.0

# Intro/Outro configuration
# Use either a combined directory or separate ones:
# INTRO_OUTRO_DIR=/media/hairzee/Dev2/AutoScribe-AI/intro_outros
# INTRO_DIR=./intro
# OUTRO_DIR=./outro
# Crossfade (ms) when concatenating (requires pydub). Default 750.
# INTRO_OUTRO_CROSSFADE_MS=750
